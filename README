add a new branch:
	git checkout -b dev
choose a branch 
	git checkout branch-name
merge two branch
	choose the branch-A
	git merge branch-name-B
delete branch 
	git branch -d branch-name

delete a remote branch 
	git push origin :delete-branch-name
get a file from origin
	git checkout -- filename

